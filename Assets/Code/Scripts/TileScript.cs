using UnityEngine;

public class TileScript : MonoBehaviour
{
    // Установите это значение в true для тайлов, на которых можно строить
    // Ваш MapGenerator должен это настраивать при создании карты
    public bool isBuildable = true;

    // Флаг, показывающий, занят ли тайл башней
    public bool hasTower = false;

    // Необязательно: Ссылка на установленную башню (если нужно будет взаимодействовать с ней через тайл)
    // public GameObject towerInstance = null;

    // Можно добавить визуализацию (например, подсветку) при наведении мыши,
    // но для начала сосредоточимся на основной функциональности.
}